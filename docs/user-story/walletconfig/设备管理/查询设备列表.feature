# language: zh-CN
功能: 查询设备列表
  作为 学校管理员，
  我希望 查看已登记的设备及其归属机构，
  以便 核验设备部署情况。

  场景: 设备列表展示所有关键字段
    假设 机构"一食堂"下存在序列号为"POS-0001"（POS机）的设备，机构"二号档口"下存在序列号为"FACE-0002"（多媒体人脸机）的设备
    当 学校管理员打开设备列表页面
    那么 列表应展示以下字段：
      | 字段名     |
      | 设备序列号 |
      | 设备类型   |
      | 归属机构   |
    并且 列表应展示 2 条记录

  场景: 设备列表默认按创建时间倒序展示
    假设 机构"一食堂"下先登记了序列号为"POS-0001"的设备，后登记了序列号为"POS-0002"的设备
    当 学校管理员打开设备列表页面
    那么 序列号"POS-0002"的设备应展示在"POS-0001"的设备之前

  场景: 按设备序列号搜索设备
    假设 机构"一食堂"下存在序列号为"POS-0001"的设备，机构"二号档口"下存在序列号为"FACE-0002"的设备
    当 学校管理员在设备序列号搜索框中输入"POS-0001"
    那么 列表应只展示序列号为"POS-0001"的设备

  场景: 设备列表为空时展示空态提示
    假设 租户下不存在任何设备
    当 学校管理员打开设备列表页面
    那么 应提示"暂无数据"
