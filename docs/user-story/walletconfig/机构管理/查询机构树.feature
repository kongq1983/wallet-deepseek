# language: zh-CN
功能: 查询机构树
  作为 学校管理员，
  我希望 以树形查看租户下的全部机构，
  以便 掌握机构的层级关系与类型分布。

  场景: 机构树展示所有关键字段
    假设 机构树中存在顶层餐厅"一食堂"，"一食堂"下存在档口"一号档口"
    当 学校管理员打开机构管理页面
    那么 机构树应展示以下字段：
      | 字段名   |
      | 机构名称 |
      | 机构类型 |
      | 上级机构 |
      | 层级     |
    并且 "一号档口"应展示在"一食堂"的下级

  场景: 机构树为空时展示空态提示
    假设 租户下不存在任何机构
    当 学校管理员打开机构管理页面
    那么 应提示"暂无数据"
