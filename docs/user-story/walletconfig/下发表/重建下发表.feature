# language: zh-CN
功能: 重建下发表
  作为 学校管理员，
  我希望 按当前配置全量重建下发表数据，
  以便 在下发表被清空或与配置不一致时恢复出正确的下发数据。

  # 下发表是派生数据，没有人工录入入口；本功能只触发按当前配置的整体重算。

  场景: 全量重建后所有数据从版本号 1 开始
    假设 租户下不存在任何下发表数据
    并且 机构"一食堂"下存在设备"POS-0001"，其可消费身份为"学生"，工作钱包为 1、追扣钱包为 2
    当 学校管理员触发全量重建
    那么 应为设备"POS-0001"生成 2 条有效数据
    并且 这 2 条数据的版本号应均为 1

  场景: 重建按当前配置生成数据
    假设 机构"一食堂"的支付参数为工作钱包 1、追扣钱包 2，且"学生"为其可消费身份
    当 学校管理员触发全量重建
    那么 应存在身份"学生"的工作钱包数据，钱包为 1
    并且 应存在身份"学生"的追扣钱包数据，钱包为 2

  场景: 重建后版本号继续按批次递增
    假设 租户下已存在版本号为 1 的下发表数据
    当 学校管理员触发全量重建且期间配置未发生变化
    那么 不应产生新的下发表数据
    并且 版本号不应发生变更
