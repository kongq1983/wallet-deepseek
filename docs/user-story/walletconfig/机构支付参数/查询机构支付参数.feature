# language: zh-CN
功能: 查询机构支付参数
  作为 学校管理员，
  我希望 查看指定机构的支付参数，
  以便 核对追扣开关、工作钱包与可消费身份等配置是否符合预期。

  场景: 机构支付参数页面展示所有关键字段
    假设 餐厅"一食堂"已配置支付参数：可追扣为开启，工作钱包为 1，身份限制为开启，可消费身份为"学生"与"老师"，脱机消费为开启
    当 学校管理员打开"一食堂"的机构支付参数页面
    那么 页面应展示以下字段：
      | 字段名           |
      | 是否可追扣       |
      | 工作钱包         |
      | 身份限制         |
      | 可消费身份       |
      | 是否允许脱机消费 |
    并且 页面应展示可追扣为"开启"、工作钱包为"1"、可消费身份为"学生/老师"

  场景: 机构尚未配置支付参数时展示未配置提示
    假设 机构树中存在餐厅"一食堂"且该机构尚未配置支付参数
    当 学校管理员打开"一食堂"的机构支付参数页面
    那么 应提示"该机构尚未配置支付参数"
