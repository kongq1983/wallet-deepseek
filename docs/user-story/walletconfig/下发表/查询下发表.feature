# language: zh-CN
功能: 查询下发表
  作为 学校管理员，
  我希望 查看系统自动生成的设备身份钱包下发数据，
  以便 核验下发到设备上的钱包配置是否与机构参数、身份参数的配置一致。

  场景: 下发表展示所有关键字段
    假设 机构"一食堂"下存在设备"POS-0001"，其可消费身份为"学生"，工作钱包为 1、追扣钱包为 2
    当 学校管理员打开下发表页面
    那么 列表应展示以下字段：
      | 字段名           |
      | 设备序列号       |
      | 交易身份         |
      | 钱包类型         |
      | 钱包             |
      | 是否允许追扣     |
      | 是否允许脱机消费 |
      | 有效标记         |
      | 版本号           |

  场景: 下发表默认按版本号倒序展示
    假设 设备"POS-0001"的下发表中存在版本号为 1 的数据与版本号为 2 的数据
    当 学校管理员打开下发表页面
    那么 版本号为 2 的数据应展示在版本号为 1 的数据之前

  场景: 同一设备的同一身份同时下发工作钱包与追扣钱包
    假设 机构"一食堂"下存在设备"POS-0001"，其可消费身份为"学生"，工作钱包为 1、追扣钱包为 2
    当 学校管理员在下发表页面查询设备"POS-0001"
    那么 应存在 2 条属于身份"学生"的数据
    并且 一条数据的钱包类型应为"工作钱包"，钱包应为 1
    并且 另一条数据的钱包类型应为"追扣钱包"，钱包应为 2

  场景: 机构不可追扣时追扣钱包数据仍然下发
    假设 机构"一食堂"下存在设备"POS-0001"，其可消费身份为"学生"，工作钱包为 1、追扣钱包为 2，且该机构的是否可追扣为"关闭"
    当 学校管理员在下发表页面查询设备"POS-0001"
    那么 应存在钱包类型为"追扣钱包"的数据
    并且 该条数据的是否允许追扣应为"关闭"

  场景: 仅查看有效标记为有效的数据
    假设 设备"POS-0001"的下发表中存在有效标记为"无效"的历史数据
    当 学校管理员在下发表页面筛选有效标记为"有效"
    那么 列表应只展示有效标记为"有效"的数据

  场景: 下发表为空时展示空态提示
    假设 租户下不存在任何下发表数据
    当 学校管理员打开下发表页面
    那么 应提示"暂无数据"
