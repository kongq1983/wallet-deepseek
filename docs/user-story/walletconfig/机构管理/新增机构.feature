# language: zh-CN
功能: 新增机构
  作为 学校管理员，
  我希望 在机构树中新增一个机构，
  以便 按餐厅、档口、超市的实际组织结构建立消费场所。

  # 表单字段规格：
  # | 字段名   | 控件类型 | 必填 | 约束                                            |
  # | 机构名称 | 文本输入 | 是   | 最大 50 字符；同一上级机构下不可重复              |
  # | 机构类型 | 下拉选择 | 是   | 可选值：餐厅/档口/超市                           |
  # | 上级机构 | 树选择   | 否   | 仅可选择第 1 层与第 2 层的机构；不选表示创建为顶层机构 |

  规则: 必须满足机构名称与机构类型均已填写才允许新增机构

    场景: 学校管理员成功新增一个顶层机构
      当 学校管理员填写新增机构表单：
        | 字段名   | 值   |
        | 机构名称 | 一食堂 |
        | 机构类型 | 餐厅 |
        | 上级机构 |      |
      而且 提交表单
      那么 机构应新增成功
      并且 机构树中应存在"一食堂"，且其上级机构为空

    场景: 学校管理员成功新增一个子机构
      假设 机构树中已存在类型为餐厅的"一食堂"
      当 学校管理员填写新增机构表单：
        | 字段名   | 值     |
        | 机构名称 | 一号档口 |
        | 机构类型 | 档口   |
        | 上级机构 | 一食堂 |
      而且 提交表单
      那么 机构应新增成功
      并且 机构树中"一号档口"应位于"一食堂"之下

  规则: 机构树的深度不得超过 3 层

    场景: 在第 3 层机构下新增子机构时失败
      假设 机构树中已存在"一食堂"及其下级"二号档口"，且"二号档口"下已存在"面食窗口"
      当 学校管理员在"面食窗口"下填写新增机构表单：
        | 字段名   | 值     |
        | 机构名称 | 凉菜窗口 |
        | 机构类型 | 档口   |
        | 上级机构 | 面食窗口 |
      而且 提交表单
      那么 应提示"机构层级不能超过3层"

  规则: 禁止为类型为档口的机构创建子机构

    场景: 在档口下新增子机构时失败
      假设 机构树中已存在类型为档口的"一号档口"
      当 学校管理员在"一号档口"下填写新增机构表单：
        | 字段名   | 值     |
        | 机构名称 | 内部窗口 |
        | 机构类型 | 档口   |
        | 上级机构 | 一号档口 |
      而且 提交表单
      那么 应提示"档口下不能创建子机构"

  规则: 机构名称必须在同一上级机构下唯一

    场景: 同一上级机构下机构名称重复时新增失败
      假设 机构树中"一食堂"下已存在名为"一号档口"的机构
      当 学校管理员在"一食堂"下填写新增机构表单：
        | 字段名   | 值     |
        | 机构名称 | 一号档口 |
        | 机构类型 | 档口   |
        | 上级机构 | 一食堂 |
      而且 提交表单
      那么 应提示"机构名称已存在"

    场景: 不同上级机构下机构名称可以相同
      假设 机构树中"一食堂"下已存在名为"一号档口"的机构，且已存在餐厅"二食堂"
      当 学校管理员在"二食堂"下填写新增机构表单：
        | 字段名   | 值     |
        | 机构名称 | 一号档口 |
        | 机构类型 | 档口   |
        | 上级机构 | 二食堂 |
      而且 提交表单
      那么 机构应新增成功

  场景: 新增机构表单展示所有字段
    假设 我打开了新增机构页面
    那么 我应该看到"机构名称"输入框，带必填*号
    并且 我应该看到"机构类型"下拉框，带必填*号
    并且 我应该看到"上级机构"树选择器

  # 机构名称字段（按字段维度）
  场景大纲: 机构名称字段校验
    假设 我在新增机构页面
    并且 其他必填字段都填写有效值
    当 我在"机构名称"输入框中输入"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值      | 提示信息             |
      | （空）      | 请输入机构名称       |
      | 51个字符的名称 | 机构名称不能超过50个字符 |

  # 机构类型字段（按字段维度）
  场景大纲: 机构类型字段校验
    假设 我在新增机构页面
    并且 其他必填字段都填写有效值
    当 我选择"机构类型"为"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值 | 提示信息     |
      | （空） | 请选择机构类型 |
