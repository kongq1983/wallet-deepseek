# language: zh-CN
功能: 新增设备
  作为 学校管理员，
  我希望 在机构下登记一台设备，
  以便 该设备可以获取其所属机构与身份对应的钱包配置数据。

  # 表单字段规格：
  # | 字段名     | 控件类型 | 必填 | 约束                              |
  # | 设备序列号 | 文本输入 | 是   | 最大 64 字符；全局唯一             |
  # | 设备类型   | 下拉选择 | 是   | 可选值：POS机/多媒体人脸机/其他     |
  # | 归属机构   | 树选择   | 是   | 可选值：机构树中的全部机构          |

  规则: 必须满足设备序列号、设备类型与归属机构均已填写才允许新增设备

    场景: 学校管理员成功新增一台设备
      假设 机构树中存在餐厅"一食堂"，且不存在序列号为"POS-0001"的设备
      当 学校管理员填写新增设备表单：
        | 字段名     | 值       |
        | 设备序列号 | POS-0001 |
        | 设备类型   | POS机    |
        | 归属机构   | 一食堂   |
      而且 提交表单
      那么 设备应新增成功
      并且 设备列表中应存在序列号"POS-0001"且归属机构为"一食堂"

  规则: 设备序列号必须全局唯一

    场景: 设备序列号重复时新增失败
      假设 已存在序列号为"POS-0001"的设备
      当 学校管理员填写新增设备表单：
        | 字段名     | 值         |
        | 设备序列号 | POS-0001   |
        | 设备类型   | 多媒体人脸机 |
        | 归属机构   | 一食堂     |
      而且 提交表单
      那么 应提示"设备序列号已存在"

  场景: 新增设备表单展示所有字段
    假设 我打开了新增设备页面
    那么 我应该看到"设备序列号"输入框，带必填*号
    并且 我应该看到"设备类型"下拉框，带必填*号
    并且 我应该看到"归属机构"树选择器，带必填*号

  # 设备序列号字段（按字段维度）
  场景大纲: 设备序列号字段校验
    假设 我在新增设备页面
    并且 其他必填字段都填写有效值
    当 我在"设备序列号"输入框中输入"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值        | 提示信息                 |
      | （空）        | 请输入设备序列号         |
      | 65个字符的序列号 | 设备序列号不能超过64个字符 |

  # 设备类型字段（按字段维度）
  场景大纲: 设备类型字段校验
    假设 我在新增设备页面
    并且 其他必填字段都填写有效值
    当 我选择"设备类型"为"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值 | 提示信息     |
      | （空） | 请选择设备类型 |

  # 归属机构字段（按字段维度）
  场景大纲: 归属机构字段校验
    假设 我在新增设备页面
    并且 其他必填字段都填写有效值
    当 我选择"归属机构"为"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值 | 提示信息     |
      | （空） | 请选择归属机构 |
