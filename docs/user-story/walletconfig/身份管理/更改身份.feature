# language: zh-CN
功能: 更改身份
  作为 学校管理员，
  我希望 修改身份的名称与是否允许脱机消费，
  以便 在身份命名有误或脱机消费策略调整时及时纠正。

  # 表单字段规格：
  # | 字段名           | 控件类型 | 必填 | 约束                    |
  # | 身份名称         | 文本输入 | 是   | 最大 50 字符；租户内不可重复 |
  # | 是否允许脱机消费 | 开关     | 是   | 默认值：关闭             |

  规则: 必须满足身份名称已填写才允许更改身份

    场景: 学校管理员成功更改身份名称
      假设 身份字典中存在名称为"学生"的身份
      当 学校管理员将"学生"的身份名称改为"在校学生"
      并且 提交表单
      那么 身份应更改成功
      并且 身份列表中应存在"在校学生"

    场景: 学校管理员成功变更身份的脱机消费参数
      假设 身份字典中存在"学生"且其脱机消费为"关闭"
      当 学校管理员将"学生"的脱机消费改为"开启"
      并且 提交表单
      那么 身份应更改成功
      并且 身份列表中"学生"的脱机消费应为"开启"

  规则: 身份名称必须在租户内唯一

    场景: 更改后的身份名称与其他身份重复时失败
      假设 身份字典中已存在"学生"与"老师"
      当 学校管理员将"老师"的身份名称改为"学生"
      并且 提交表单
      那么 应提示"身份名称已存在"

  场景: 更改身份表单展示所有字段
    假设 我打开了"学生"的修改页面
    那么 我应该看到"身份名称"输入框，带必填*号
    并且 我应该看到"是否允许脱机消费"开关

  # 身份名称字段（按字段维度）
  场景大纲: 身份名称字段校验
    假设 我在更改身份页面
    并且 其他必填字段都填写有效值
    当 我在"身份名称"输入框中输入"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值      | 提示信息             |
      | （空）      | 请输入身份名称       |
      | 51个字符的名称 | 身份名称不能超过50个字符 |
