{
  "name": "ai-engineering-guide",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "turbo run dev",
    "dev:web": "pnpm --filter frontend-admin-web dev",
    "build": "turbo run build",
    "build:web": "pnpm --filter frontend-admin-web build",
    "lint": "turbo run lint"
  }
}
