# language: zh-CN
功能: 查询身份列表
  作为 学校管理员，
  我希望 查看租户下的全部身份，
  以便 掌握可用于机构可消费身份配置的身份范围。

  场景: 身份列表展示所有关键字段
    假设 身份字典中存在"学生"（脱机消费开启）与"老师"（脱机消费关闭）
    当 学校管理员打开身份列表页面
    那么 列表应展示以下字段：
      | 字段名           |
      | 身份名称         |
      | 是否允许脱机消费 |
    并且 列表应展示 2 条记录

  场景: 身份列表为空时展示空态提示
    假设 身份字典中不存在任何身份
    当 学校管理员打开身份列表页面
    那么 应提示"暂无数据"
