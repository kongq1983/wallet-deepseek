# language: zh-CN
功能: 创建身份
  作为 学校管理员，
  我希望 创建一个新的身份，
  以便 在机构的可消费身份配置中引用该身份。

  # 表单字段规格：
  # | 字段名           | 控件类型 | 必填 | 约束                    |
  # | 身份名称         | 文本输入 | 是   | 最大 50 字符；租户内不可重复 |
  # | 是否允许脱机消费 | 开关     | 是   | 默认值：关闭             |

  规则: 必须满足身份名称已填写才允许创建身份

    场景: 学校管理员成功创建一个身份
      假设 身份字典中不存在名称为"学生"的身份
      当 学校管理员填写创建身份表单：
        | 字段名           | 值   |
        | 身份名称         | 学生 |
        | 是否允许脱机消费 | 开启 |
      而且 提交表单
      那么 身份应创建成功
      并且 身份列表中应存在"学生"，且其脱机消费为"开启"

  规则: 身份名称必须在租户内唯一

    场景: 身份名称重复时创建失败
      假设 身份字典中已存在名称为"学生"的身份
      当 学校管理员填写创建身份表单：
        | 字段名           | 值   |
        | 身份名称         | 学生 |
        | 是否允许脱机消费 | 关闭 |
      而且 提交表单
      那么 应提示"身份名称已存在"

  场景: 创建身份表单展示所有字段
    假设 我打开了创建身份页面
    那么 我应该看到"身份名称"输入框，带必填*号
    并且 我应该看到"是否允许脱机消费"开关

  # 身份名称字段（按字段维度）
  场景大纲: 身份名称字段校验
    假设 我在创建身份页面
    并且 其他必填字段都填写有效值
    当 我在"身份名称"输入框中输入"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值      | 提示信息             |
      | （空）      | 请输入身份名称       |
      | 51个字符的名称 | 身份名称不能超过50个字符 |
