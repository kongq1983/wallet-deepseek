# language: zh-CN
功能: 保存机构支付参数
  作为 学校管理员，
  我希望 为机构设置支付相关参数，
  以便 控制该机构下的消费可以使用哪个工作钱包、是否允许追扣、哪些身份可以消费以及是否允许脱机消费。

  # 表单字段规格：
  # | 字段名           | 控件类型 | 必填 | 约束                                             |
  # | 是否可追扣       | 开关     | 是   | 默认值：关闭                                      |
  # | 工作钱包         | 下拉选择 | 是   | 可选值：租户钱包配对中已配置的工作钱包编号          |
  # | 身份限制         | 开关     | 是   | 默认值：关闭                                      |
  # | 可消费身份       | 多选     | 否   | 当身份限制=开启时必填；可选值：身份字典中的全部身份  |
  # | 是否允许脱机消费 | 开关     | 是   | 默认值：关闭                                      |

  规则: 每个机构的支付参数必须设置且仅设置一个工作钱包

    场景: 学校管理员成功保存机构支付参数
      假设 机构树中存在餐厅"一食堂"，且租户钱包配对中存在"工作钱包 1 ↔ 追扣钱包 2"
      当 学校管理员为"一食堂"填写机构支付参数表单：
        | 字段名           | 值     |
        | 是否可追扣       | 开启   |
        | 工作钱包         | 1      |
        | 身份限制         | 关闭   |
        | 可消费身份       |        |
        | 是否允许脱机消费 | 开启   |
      而且 提交表单
      那么 机构支付参数应保存成功
      并且 "一食堂"的工作钱包应为 1，追扣钱包应为 2

  规则: 机构支付参数的工作钱包必须取自租户钱包配对中已配置的工作钱包编号

    场景: 工作钱包未选择时保存失败
      假设 机构树中存在餐厅"一食堂"，且租户钱包配对中存在"工作钱包 1 ↔ 追扣钱包 2"
      当 学校管理员为"一食堂"填写机构支付参数表单：
        | 字段名           | 值   |
        | 是否可追扣       | 关闭 |
        | 工作钱包         |      |
        | 身份限制         | 关闭 |
        | 可消费身份       |      |
        | 是否允许脱机消费 | 关闭 |
      而且 提交表单
      那么 工作钱包字段应提示"请选择工作钱包"

  规则: 必须满足身份限制开启时可消费身份已选择才允许保存机构支付参数

    场景: 身份限制开启时选择多个可消费身份并保存成功
      假设 机构树中存在餐厅"一食堂"，租户钱包配对中存在"工作钱包 1 ↔ 追扣钱包 2"，身份字典中存在"学生"与"老师"
      当 学校管理员为"一食堂"填写机构支付参数表单：
        | 字段名           | 值       |
        | 是否可追扣       | 关闭     |
        | 工作钱包         | 1        |
        | 身份限制         | 开启     |
        | 可消费身份       | 学生/老师 |
        | 是否允许脱机消费 | 关闭     |
      而且 提交表单
      那么 机构支付参数应保存成功
      并且 "一食堂"的可消费身份应为"学生"与"老师"

    场景: 身份限制开启但未选择可消费身份时保存失败
      假设 机构树中存在餐厅"一食堂"，租户钱包配对中存在"工作钱包 1 ↔ 追扣钱包 2"
      当 学校管理员为"一食堂"填写机构支付参数表单：
        | 字段名           | 值   |
        | 是否可追扣       | 关闭 |
        | 工作钱包         | 1    |
        | 身份限制         | 开启 |
        | 可消费身份       |      |
        | 是否允许脱机消费 | 关闭 |
      而且 提交表单
      那么 可消费身份字段应提示"请选择可消费身份"

  场景: 机构支付参数表单展示所有字段
    假设 我打开了"一食堂"的机构支付参数页面
    那么 我应该看到"是否可追扣"开关
    并且 我应该看到"工作钱包"下拉框，带必填*号
    并且 我应该看到"身份限制"开关
    并且 我应该看到"可消费身份"多选框组
    并且 我应该看到"是否允许脱机消费"开关

  # 工作钱包字段（按字段维度）
  场景大纲: 工作钱包字段校验
    假设 我在机构支付参数页面
    并且 其他必填字段都填写有效值
    当 我选择"工作钱包"为"<输入值>"
    并且 我点击"保存"
    那么 我应该看到"<提示信息>"

    例子:
      | 输入值 | 提示信息     |
      | （空） | 请选择工作钱包 |
